[project]
name = "Controller Binds"
version = "1.0.0"
authors = [{name = "Siggles"}]
description = """Adds controller combo binds for any mod keybinds, adds mod menu bind, and fixes the controller rebind menu in BL2.
\n
All controller binds are set in <i>this<\i> mod's keybind options.
"""

[tool.sdkmod]
license = {name = "GPL3", url = "https://choosealicense.com/licenses/gpl-3.0/" }
#supported_games = ["BL2", "TPS", "AoDK"]
